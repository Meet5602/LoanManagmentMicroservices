server:
  port: 8083

spring:
  cloud:
    gateway:
      routes:
        - id: user-auth-service
          uri: http://localhost:8082
          predicates:
            - Path=/auth/**, /admin/**

        - id: loan-management-service
          uri: http://localhost:8081
          predicates:
            - Path=/api/loans/**, /api/admin/**, /swagger-ui/**, /v3/api-docs/**