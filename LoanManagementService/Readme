While fetching the user data we have implemented the data fetching for all users and all loans we wont call for each all users and then all loans for each user we will do batch fetching for all users and all loans and then we will do the mapping in memory to get the required data for each user and their loans. This way we can reduce the number of calls to the database and improve the performance of our application.